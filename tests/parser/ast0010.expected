{ VARIABLE, line 1, .name = MAKE_ENV, .modifier = +=, .words = { CXXFLAGS_WARN="${CXXFLAGS_WARN}" } }
{ VARIABLE, line 2, .name = MAKE_ENV, .modifier = +=, .words = { GNUSED=${LOCALBASE}/bin/gsed } }
{ VARIABLE, line 3, .name = MAKE_ENV, .modifier = +=, .words = { DISPLAY= } }
{ VARIABLE, line 4, .name = MAKE_ENV, .modifier = +=, .words = { verbose=1 } }
